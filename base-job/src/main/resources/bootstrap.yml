server:
  port: 8057
  servlet:
    context-path: /xxl-job-admin

spring:
  application:
    name: base-job
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        metadata:
          management.context-path: /xxl-job-admin/actuator
        namespace: dev
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        namespace: dev
